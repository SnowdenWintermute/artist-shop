@inherits LayoutComponentBase

@* For a page shown inside another page's frame, like the post editor's artwork picker: no site
   header, since the page around it already has one *@
<div class="min-h-dvh bg-white p-4">
  @Body
</div>
