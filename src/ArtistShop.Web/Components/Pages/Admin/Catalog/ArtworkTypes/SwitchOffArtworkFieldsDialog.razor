@using ArtistShop.Web.Components.Dialogs
@using ArtistShop.Web.Domain.Catalog
@using ArtistShop.Web.Utilities

@code {
  [Parameter]
  public bool Open { get; set; }

  [Parameter]
  public EventCallback<bool> OpenChanged { get; set; }

  [Parameter, EditorRequired]
  public IReadOnlyList<ArtworkFieldDefinition> SwitchedOffFields { get; set; } = default!;

  [Parameter, EditorRequired]
  public ArtworkTypeArtworkCounts ArtworkCounts { get; set; } = default!;

  [Parameter, EditorRequired]
  public EventCallback OnConfirm { get; set; }

  [Parameter]
  public bool IsWorking { get; set; }

  private string ClearedValues =>
    string.Join(", ", SwitchedOffFields.Select(definition =>
      $"{definition.Name} on {CountText.Of(ArtworkCounts.WithValueIn(definition.Field), "artwork")}"));
}

<ConfirmDialog Open="Open" OpenChanged="OpenChanged" Title="Clear values from artworks?"
               ConfirmLabel="Save and clear" OnConfirm="OnConfirm" IsWorking="IsWorking">
  Switching these fields off clears @ClearedValues (count as of page load).
</ConfirmDialog>
