# artshop.mikesilverman.net: the VPS's nginx server block for the shop, which listens on
# localhost:8089 (see vps.docker.yml in the speed-dungeon repo). Copy to
# /etc/nginx/sites-available/artshop, link it into sites-enabled, then run
#   certbot --nginx -d artshop.mikesilverman.net
# which adds the listen 443 and certificate lines and the redirect from http.

server {
        server_name artshop.mikesilverman.net;

        # an image upload is at most 25 MiB plus the form around it
        # (ImageUploadValidation.MaximumRequestBytes); nginx's default is 1 MB
        client_max_body_size 26m;

        location / {
             proxy_pass http://localhost:8089;
             proxy_set_header Host $host;
             proxy_set_header X-Forwarded-For $remote_addr;
             proxy_set_header X-Forwarded-Proto $scheme;
             # an upload can queue behind others for up to ImageProcessing:ProxyTimeout
             proxy_read_timeout 60s;
        }

        # the websocket the interactive islands talk over. Its own block because only a websocket
        # should be sent Connection: upgrade; the timeouts keep an idle circuit's socket open
        location /_blazor {
             proxy_pass http://localhost:8089;
             proxy_http_version 1.1;
             proxy_set_header Upgrade $http_upgrade;
             proxy_set_header Connection "upgrade";
             proxy_set_header Host $host;
             proxy_set_header X-Forwarded-For $remote_addr;
             proxy_set_header X-Forwarded-Proto $scheme;
             proxy_read_timeout 3600s;
             proxy_send_timeout 3600s;
        }

        listen 80;
}
